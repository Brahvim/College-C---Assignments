#include "BrahvimIoStreamUtils.hpp"

int main() {

    std::cout << "Enter a number between `1` and `5`." << std::endl;
    declare_then_get_via_cin(char, uin);

    switch (uin) {

        default:
        std::cout << "" << std::endl;
        break;
    }

}
