#include <iostream>
#include <string>

using namespace std;

int main() {
    string first = "Brahvim";
    string last = "Bhaktvatsal";
    string full = first.append(" ").append(last);
    cout << full;
}
